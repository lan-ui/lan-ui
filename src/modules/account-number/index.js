import AccountNumber from '../../components/account-number/account-number.vue'

AccountNumber.install = function (Vue) {
  Vue.component(AccountNumber.name, AccountNumber)
}

export default AccountNumber
