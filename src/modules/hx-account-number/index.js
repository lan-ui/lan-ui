import HxAccountNumber from '../../components/hx-account-number/hx-account-number.vue'

HxAccountNumber.install = function (Vue) {
  Vue.component(HxAccountNumber.name, HxAccountNumber)
}

export default HxAccountNumber
